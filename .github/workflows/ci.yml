name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Build and test
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential binutils dpkg-dev file

      - name: Run tests
        run: |
          make clean
          make test

      - name: Build native Debian package
        run: |
          make clean
          make package-deb

      - name: Upload native Debian package
        uses: actions/upload-artifact@v4
        with:
          name: runpod-shim-native-deb
          path: build/*.deb

  compatible-package:
    name: Ubuntu 20.04 compatible package
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build compatible Debian package
        run: make package-deb-compatible

      - name: Upload compatible Debian package
        uses: actions/upload-artifact@v4
        with:
          name: runpod-shim-compatible-deb
          path: build/*.deb
